
STATIC METHOD
   - The methods which are defined
   without self or cls and it should be
   defined with @staticmethod

   - Instance method can perform the operations
   on both static and non static fields
   - class methods can perform the operations
   on only static variables
   - static method can't perform the operations on
   static or instance fields

   - static methods can be referred by self or classname
   throughout the class.

   - static methods can be referred by classname
   or object reference whenever u want access it
   from outside of the class.
   





   
