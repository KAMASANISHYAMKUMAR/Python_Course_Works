
Instance Methods
   1.Methods which are defined
   with in the class with self as
   first argument.

   2.Every instance method must be referred by
   an object reference whenever u want access it
   from outside of the class.

   3.Every instance method must be referred by
   self throughout the class.

   4.Instance methods can perform the operations
   on both static and non static field | instance fields
   
