
SELF
    - self is not a keyword
    
    - If we define any function in the
       class called method, The first argument
       of method must be self.

    - self is always referred to the current class
    or current class object reference

    - self looks like formal parameter but doesn't
       works like formal parameter

    - self is acts a local variable.
    

    
