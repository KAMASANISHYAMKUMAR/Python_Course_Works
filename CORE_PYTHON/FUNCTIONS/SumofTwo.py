#defining
def sum(x,y):
    s=x+y
    print("Sum is : ",s)
    print("==========")

#calling
sum(10,20)
sum(5+5+5,10*3)
a=90
b=90
sum(a,b)


''' What is diff b|w formal parameters vs actual args
Formal parameters
    - Parameters which are declared in the function def
    - Formal parameters must be variables
    - Formal parameters are acts as local variables
    - Formal parameters are copies of actual args

Actual args:
    - The args which we are passing by the time of
       calling the function
    - actual args may be values | variables | exp  '''
    







    
