
Command Line Arguments :
    We can prompt the inputs from the user
            By using input( )
            By using Command line arguments

* Process Of passing the values to the program
by the time execution along with prgname through
command prompt

* We can pass N no.of. argument and any type of
arguments but each value should be sep by a space

* String values are recommanded to take in between " "

* The values which we are passing through command
prompt those value are stored in the '''argv variable
of class list from sys module'''.

* argv[0] always holds file name.
* argv[1]..... argv[n] holds all command line arguments.


* All the command line arguments are treated
by the system as string only


  
